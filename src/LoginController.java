import javax.swing.*;

/**
 * Created by Louise Windows on 18-04-2017.
 */
public class LoginController {

    /*
    public void loginValidation(String user, String password) {
        UserRoles role = getUserRole(user);
        boolean isValid = validateLogin(user, password);
        if (DBController.equals UserRoles.MANAGEMENT && isValid){
            JOptionPane.showMessageDialog(null, "Logged in as management");
        } else if (DBController. equals UserRoles.STAFF && isValid) {
            JOptionPane.showMessageDialog(null, "Logged in as staff");
        } else if (!isValid) {
            JOptionPane.showMessageDialog(null, "Log in failed, username and " +
                    "password does not match!");
        }
    }
*/
    private boolean validateLogin(String user, String password) {

        return true;
    }
}
